def multiplier(num):
    result = int(num) * 100
    return result

    """
    We can also use lambda expressions too
    Syntax
    lambda arguments : expression
    #return lambda x: num * x
    """